
@{
    ViewData["Title"] = "Manage Roles";
    IdentityUser user = (IdentityUser)ViewBag.User;
}

<h4>Roles of User @user.UserName</h4>
<hr />
<div class="row">
    @Html.ValidationSummary(false, "", new { @class = "text-danger" })
    <form asp-action="AddEditUsersInRole" asp-route-roleId="@user.Id">

        @*@for (int i = 0; i < Model.Count(); i++)
        {

            <div class="form-check m-1">
                <input hidden asp-for="@Model[i].UserId">
                <input hidden asp-for="@Model[i].UserName">
                <input class="form-check-input" asp-for="@Model[i].IsSelected">
                <label class="form-check-label" asp-for="@Model[i].IsSelected">
                    @Model[i].UserName
                </label>
            </div>

        }*@
        <hr />
        <input type="submit" class="btn btn-primary" value="Update" />
        <a class="btn btn-default"
           asp-controller="Administration"
           asp-action="EditUser"
           asp-route-id="@user.Id">Cancel</a>
    </form>
</div>