function validateAndSubmit(event) {
    debugger
    var isFormValid = true;

    if ($('#RoleId').val() === '1') {
        if ($('#FirstName').val() === '') {

        }
    }







    //event.preventDefault();

    //$("#SignUpForm").validate({
    //    submitHandler: function (form) {
    //        $(form).ajaxSubmit();
    //    }
    //});
}